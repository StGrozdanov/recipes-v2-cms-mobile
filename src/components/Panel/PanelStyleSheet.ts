import { StyleSheet } from "react-native";

export const adminPanel: Record<string, any> = StyleSheet.create({
    lightContainer: {
        backgroundColor: '#EFEEFE',
        flex: 1,
    },

    darkContainer: {
        backgroundColor: '#111',
        flex: 1,
    }
});